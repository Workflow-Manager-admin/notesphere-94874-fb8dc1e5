# Public Assets

Anything in this folder will be served statically at the root URL in production (/).

- `index.html`: Entry point for React app, controls meta tags, app manifest, and theme color.
- `favicon.ico`: App browser tab icon. Update for branding presence.
